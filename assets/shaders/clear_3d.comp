#version 430
layout(local_size_x = 8, local_size_y = 8, local_size_z = 8) in;

layout(location = 0) restrict writeonly uniform image3D toClear;

void main() {
    ivec3 pCoord = ivec3(gl_GlobalInvocationID);
    imageStore(toClear, pCoord, vec4(0.0));
}
